# 信水義河互動地圖 更新日誌

## [v1.12.0] - 2026-09-21 - 🌐 - FEATURE - 英文版 /en：完整翻譯的雙語網站與語言切換
1. **新增 /en 英文版子網站**：首頁、地圖、熱舒適問卷、隱私聲明、更新日誌全部提供精準人工翻譯的英文版，不是機器翻譯外掛。中英文共用同一套元件與同一份地圖資料，內容只維護一次。
2. **語言切換按鈕**：首頁、地圖頁、問卷頁、隱私頁右上角都有「中／EN」切換。切換時會帶著目前畫面狀態（是否已離開首頁動畫、正在看哪個分頁），不會把人丟回首頁重來。不做瀏覽器語言自動跳轉，網址與畫面完全可預測。
3. **四條路線 57 個站點全部翻譯**：站名、導語與完整內文都有英文版，圖說一併翻譯。專有名詞依既定規則處理——行政地名用漢語拼音、水名用台語羅馬字（瑠公圳＝Liu-kong-tsun，首次出現註明官方拼法 Liugong）；查不到可靠讀音的水名（允龜陂、舊陂溪等）保留中文原名加英文類名，不憑音自造拼寫。捷運站、學校、宮廟等名稱逐一查證官方英文名。
4. **問卷英文版直通同一份表單**：英文介面送出的選擇題值仍是原本的中文字串，因此英文回覆與中文回覆落在同一張表單的同一個選項下，後台分析不需要任何改動。英文介面下地點搜尋改用英文地名，語音輸入改用英文辨識。
5. **更新日誌粗體修復**：內容行的 `**粗體**` 過去會原樣顯示星號，現已正確渲染（中英版同時生效）。
6. **手機版頁首調整**：加入語言鈕後，375px 寬度下副標改為隱藏，確保問卷、隱私、首頁三個按鈕不會被擠出畫面。

## [v1.11.0] - 2026-08-28 - 🌲 - FEATURE - 新增「公園綠地與樹林」圖層
1. **公開地圖新增「公園綠地與樹林」圖層**：原本的「行道樹遮蔭」只涵蓋道路兩旁的行道樹，象山、四獸山與各公園一帶完全空白——那些正是走讀時最有樹蔭的地方。新圖層以 OpenStreetMap 的公園、花園、草地、樹林等面狀範圍補上這片空白，與行道樹分為兩層並列（一為個別樹木點位、一為綠地範圍，性質不同故不合併），點選可查看綠地名稱與類型。
2. **圖層說明補充資料範圍**：「行道樹遮蔭」的說明加註「僅道路兩旁，不含公園」，避免誤讀綠覆蓋情形。
3. **地圖圖層面板高度調整**：面板改以所在容器計算高度，在各種螢幕與嵌入情境下都不會超出地圖範圍。
4. **內部研究工具**：新增供計畫團隊使用的熱舒適問卷分析後台（需通行密碼，不對外開放）。

## [v1.10.8] - 2026-08-28 - ☀️ - UPDATE - 日照陰影更準確：修正凹形建築與樹影失真
1. **凹形建築的陰影不再被填實**：原本把建築輪廓與投影後的屋頂合併取「凸包」，遇到 L 形街屋、口字形公寓（中央有天井）時，凹陷處會被錯誤填滿。改為沿每條邊掃出陰影帶，天井與凹角得以保留。以 L 形建築實測驗證：舊算法會填實凹角，新算法不會。
2. **樹影不再過粗**：樹冠懸在數公尺高空，原本卻把地面輪廓一併算入陰影，使樹影像一條由樹幹底部拖出的粗管。改為只投影樹冠本身，樹影回復成一團一團的自然形狀。
3. **建築圖資載入更穩定**：地圖移動改為延遲 0.5 秒才發送查詢（避免拖曳時連續轟炸圖資伺服器）；相同視野範圍會重用先前結果，不重複查詢；查詢失敗時自動重試一次；縮放回到可顯示範圍或視窗大小改變後，也會重新取得資料，不會再卡在空白狀態。
4. **新增載入狀態提示**：圖資查詢需要數秒，過去畫面毫無反應。現在會顯示「正在載入建築輪廓…」，重試中、載入失敗、以及需要再放大地圖時也都有對應提示。
5. **標示建築高度的資料來源**：陰影長度取決於建築高度，而 OpenStreetMap 未必每棟都有標註。圖層說明新增註記：未標註者以 12 公尺（約 4 層樓）估算，陰影長度僅供參考。

## [v1.10.7] - 2026-08-28 - 🧹 - UPDATE - 移除未使用的 Puppeteer 相依套件，清理過時除錯檔
1. **移除 `puppeteer`**：此套件只服務 2026 年 5 月一次性的除錯腳本，網站本身完全沒有用到，卻獨力貢獻了安全稽核中 8 個高風險項目的全部。移除後高風險項目由 8 項降為 2 項（皆為 ESLint 工具鏈的間接相依，不影響網站執行），相依套件減少 78 個，安裝體積約少 49 MB，部署建置也會更快。
2. **清理過時除錯檔案**：刪除 `test_crash.js`、`test_crash2.js`、`test-cluster.js`。移除前已重新驗證：當初這些腳本要追查的圖層當機問題已不存在——同時開啟「行道樹遮蔭」與「人行道範圍」兩個圖層（資料合計約 6 MB），頁面無任何錯誤、記憶體用量 22 MB、地圖運作正常。
3. **移除另外兩個未使用的相依套件**：`react-leaflet-cluster`（程式碼中完全沒有引用）與 `leaflet-shadow-simulator`（日照陰影圖層其實是自行實作的——以 SunCalc 計算太陽方位角與高度角，再將 OSM 建築輪廓與行道樹樹冠投影成陰影多邊形，從未使用該套件）。
4. **移除失效的環境變數**：`NEXT_PUBLIC_SHADEMAP_API_KEY` 原本由地圖元件傳入日照圖層，但圖層內部完全沒有使用，屬於無作用的設定。已從程式碼、說明文件與環境設定檔中移除（Vercel 上的同名變數也可一併刪除）。

## [v1.10.6] - 2026-08-27 - 🗺️ - UPDATE - 底圖修復：支援 CARTO API Key，解決全圖浮水印
1. **修復地圖浮水印**：CARTO 自 2026 年 8 月起要求底圖 API Key，未帶 Key 的圖磚會被斜印滿「API KEY REQUIRED」字樣，主地圖與問卷的三張地點小地圖皆受影響，街名幾乎無法辨識。現已改為統一從環境變數 `NEXT_PUBLIC_CARTO_KEY` 帶入金鑰；未設定時自動沿用原本網址，功能不受影響。
2. **底圖設定集中管理**：新增 `src/lib/basemap.js`，主地圖與問卷小地圖共用同一份底圖網址與版權標示，日後調整底圖只需修改一處。
3. **資料前處理腳本可跨平台執行**：`processTrees.js` 原本寫死 Windows 路徑，改為以專案相對路徑計算，並可用 `node processTrees.js <檔案路徑>` 指定輸入檔。
4. **升級 Next.js 至 16.3.3**：修補官方安全公告中的 Server Components 阻斷服務（DoS）等風險，並連帶更新 PostCSS 等相依套件；已重跑正式建置與全站功能驗證，畫面與功能不變。
5. **修復正式建置被 TypeScript 擋下的問題**：`tsconfig.json` 的 `baseUrl` 在 TypeScript 6 已被列為錯誤級棄用，導致 `npm run build` 中斷。改以 `paths` 單獨設定路徑別名（`@/*`），建置恢復正常。

## [v1.10.5] - 2026-08-22 - 🧭 - UI - 地點題改兩步式標示：移動地圖≠作答
1. **兩步式引導**：每題地圖上方明確分為「① 先移動地圖到大概位置（這一步只是移動畫面，還不算作答）」與「② 在地圖上點一下標記地點，出現圖釘才算完成這一題」，快跳與搜尋收進獨立灰框，與作答區視覺區隔——避免受訪者按完地標快跳就以為答完了。
2. **圖示調整**：地標快跳按鈕由 📍 改為 🧭（導航意象），不與作答的圖釘混淆。

## [v1.10.4] - 2026-08-22 - 🧭 - FEATURE - 問卷地圖找路三招：地標快跳、地點搜尋、預設放大
1. **地標快速跳轉**：每張小地圖上方新增七個地標按鈕（台北101、市政府站、象山站、永春站、後山埤站、六張犁站、吳興街），點一下即跳到該處並放大至 16 級——此級別會顯示大部分街名路名，解決底圖在小比例尺下地名稀疏、難以定位的問題。
2. **地點搜尋**：新增搜尋欄（Nominatim），輸入街名或地標即跳到該處；優先命中信義區範圍，找不到時自動放寬至臺北市重試，再找不到會給出改寫建議。
3. **預設縮放提高**：初始 zoom 由 14 提高至 15。
4. **修正跳轉失效**：flyTo 動畫在部分環境會被取消導致地圖不動，改用 setView 即時跳轉（對年長使用者也更直觀）。

## [v1.10.3] - 2026-08-22 - 🗺️ - AESTHETICS - 問卷小地圖改用主地圖同款底圖
1. **底圖統一**：問卷三張點選小地圖由原生 OSM（資訊過雜）改為主地圖同款 CARTO light 底圖，並套用相同的水文色調濾鏡（`map-tiles-tinted`），畫面乾淨、地名易讀，與全站視覺一致。

## [v1.10.2] - 2026-08-22 - 🐛 - UI - 問卷頁修復：無法滾動＋量表說明放大
1. **修復問卷頁無法滾動**：全域樣式為全屏地圖鎖定 `html/body overflow:hidden`，導致 `/survey` 整頁卡死無法捲動、無法填完——改為頁面自成滾動容器（`h-dvh overflow-y-auto`），實測手機與桌面皆可順暢捲動。
2. **量表兩極說明放大**：原本 11px 灰色小字幾乎看不見，改為醒目彩色標籤「🥵 1 分＝悶熱＆難以久待」（玫瑰紅）與「😌 5 分＝涼爽＆舒適宜人」（天藍），置於按鈕上方，並加紅→藍漸層色帶與「← 愈悶熱／愈涼爽 →」方向提示，所有人一眼明白。

## [v1.10.1] - 2026-08-22 - 🔧 - UPDATE - 問卷代交精簡：直接提交 Google 表單
1. **精簡 `/api/survey-submit`**：實測確認表單關閉「收集電子郵件」後，僅需直接 POST entry 欄位即可成功提交，移除先抓 viewform 解析 fbzx/tag token 的流程（減少一次外部請求，更快更穩）。
2. **成功判斷校正**：以實際確認頁字眼（「我們已經收到你回覆的表單」「提交其他回應」）判斷提交成功，杜絕假成功。

## [v1.10.0] - 2026-08-21 - 🌡️ - FEATURE - 熱舒適經驗調查問卷：地圖點選作答，直通 Google 表單
1. **新增 `/survey` 問卷頁**：完整承載「信義區體感溫度地圖：熱舒適經驗調查」七道題目，配合居民活動與研討會使用；填答結果直接送入原 Google 表單回覆（formResponse 對接），後台照常於 Google 表單檢視。
2. **OSM 地圖點選作答**：三道地點題（最熱、最涼爽、優先改善）各配一張 OpenStreetMap 小地圖，點選即自動反查中文地名（Nominatim）帶入欄位，並將經緯度一併寫入答案，方便後續空間分析；亦可直接手動輸入文字作答。
3. **首頁雙入口**：Landing 頁新增橙色「🌡️ 填寫熱舒適問卷」主按鈕，頂部 Header 常駐醒目問卷捷徑，活動期間居民一眼可見。
4. **容錯設計**：送出失敗時提供原 Google 表單備援連結；地名反查失敗仍保留座標記錄。

## [v1.9.7] - 2026-07-02 - 🐛 - UPDATE - 修正 AI 語氣錯位：環境通報改用客觀稽查語氣
1. **傳遞回饋性質給 AI**：表單的「AI 圖片轉譯」與「AI 文字整理」呼叫現會一併傳送 `feedbackType`，讓後端得知目前是「地方記憶」還是「環境通報」。
2. **通報專用 Prompt**：`describe-image` 與 `summarize` 兩支 API 依 `feedbackType` 切換提示詞——環境通報改用中立、精確、如稽查紀錄的語氣（描述問題徵狀、嚴重度線索、不臆測成因），取代原本一律套用的「溫暖懷舊導覽」語氣；地方記憶維持人文故事風格不變。
3. **修正結果**：通報污染、積水等問題時，AI 產出的文字不再是抒情導覽腔，而是可直接供機關參考的客觀描述。

## [v1.9.6] - 2026-07-02 - 💧 - AESTHETICS - 水的型態呼應圳水主題：有方向的流、蜿蜒的水脈軌跡
1. **圳道水脈線**：新增三條蜿蜒穿行畫面的虛線水路，以 `stroke-dashoffset` 動畫緩緩前行——潛流在城市街道之下、不曾消失的瑠公圳軌跡，與「以水文地景為經緯」的主題文案相互呼應。
2. **流光取代閃爍**：光斑由原地明滅改為順流滑行漸隱（`streak-drift`），18 道光痕沿一致方向前進——渠道裡的活水有方向、有力量，不同於海面的漫射波光。
3. **波形平緩化與流速提升**：三層波改為週期長、波幅小的流暢曲線（水順著微幅地勢起伏），流速由 16–24 秒縮短至 11–18 秒且統一流向，展現圳水的力量感。
4. **稻浪意象**：底部波帶疊入一層淡黃綠（`rgba(217,240,196,0.30)`），呼應圳水穿梭於台北盆地稻浪間的歷史地景。
5. **清澈感**：水面流紋改為橫向拉長的流線形光斑並統一漂移方向，減少模糊半徑，整體更透亮。

## [v1.9.5] - 2026-07-02 - 🌊 - AESTHETICS - 首頁水波真實感升級：碎波光、水下光網與波浪起伏
1. **碎波光（glint）**：以 22 個橫向細碎光斑取代原本的大橢圓光暈，各自不同節奏地明滅與橫向拉伸，模擬陽光在波峰碎裂的「碎銀」反光——這是真實水面最具辨識度的視覺特徵。
2. **水下光網（caustics）**：新增兩層以 CSS radial-gradient 生成的光斑網，以不同速度、方向對角漂移並以 soft-light 混合，重現水底光影緩慢流動的質感，位移量對齊圖樣週期實現無縫循環。
3. **波浪升級**：三層波改用多諧波不規則路徑（起伏更自然）、浪尖加白色高光描邊，並疊加 `wave-bob` 上下浮動動畫，水平流動＋垂直起伏的複合運動更接近真實水面。
4. **零載入負擔**：全部效果以純 CSS transform/opacity 動畫實現（GPU 合成、不觸發重排），無圖片、無外部函式庫、無 JS 動畫迴圈，首頁載入速度不受影響；並支援 `prefers-reduced-motion` 自動停用動畫。
5. **修復 Hydration 警告**：碎波光座標改用確定性偽隨機生成並固定兩位小數精度，消除 SSR 與瀏覽器端浮點數序列化差異造成的 hydration mismatch。

## [v1.9.4] - 2026-07-02 - 📤 - FEATURE - 後台資料匯出：CSV 下載與 AI 通報彙整摘要
1. **匯出 CSV**：管理後台新增「📊 匯出 CSV」按鈕，將當前篩選範圍（狀態分頁＋類型篩選）的回饋紀錄一鍵下載為 CSV 檔（含座標、類型、描述、照片連結等 12 欄），檔案帶 UTF-8 BOM 確保 Excel 開啟中文不亂碼，供資料備份與送交機關使用。
2. **AI 通報彙整摘要**：新增「📋 產生通報彙整」按鈕，將篩選範圍內的環境通報交由 Gemini 產生正式格式的彙整報告——含彙整概況、類型統計、重點通報摘述（保留經緯度座標供機關定位）與建議關注事項，可一鍵複製全文，作為定期送交臺北市環保局／水利處的基礎文件。
3. **彙整 API**：新增 `/api/admin/report-summary`（管理密碼保護），伺服器端僅擷取必要欄位進入 AI prompt、單次上限 100 筆，嚴格要求 AI 不得虛構資料中沒有的內容。

## [v1.9.3] - 2026-07-02 - 🔧 - UPDATE - GAS 後台整合：單一部署服務雙分頁（地景標記＋路線評分）
1. **合併為單一 Apps Script 部署**：`src/lib/gas-template.js` 現同時處理「地景標記回饋」與「路線舒適度評分」兩種資料，依 `payload.formType`／`action` 自動路由到同一份試算表的對應分頁，不再需要兩個獨立的 GAS 專案與部署。
2. **doGet 依 `?sheet=` 參數分流**：讀取請求可用 `?sheet=route` 指定讀取路線評分分頁，省略則預設回傳地景標記分頁（向下相容既有的 `/api/feedback-list`、`/api/admin/feedback` 呼叫）。
3. **`route-comfort-stats` API 更新**：呼叫 GAS 時自動附加 `?sheet=route`，改為讀取共用部署下的路線評分分頁。
4. **移除獨立的路線評分範本**：刪除 `src/lib/gas-route-template.js`（邏輯已併入 `gas-template.js`），避免兩份範本各自演進造成的不一致。
5. **`.env.example` 更新**：`NODE_SHEETS_API_URL` 與 `SHEETS_API_URL` 現應填入「同一個」部署網址並附上說明；順手清除檔案中殘留、未加註解的舊部署網址。

## [v1.9.2] - 2026-07-02 - 🎨 - AESTHETICS - 舒適度色階細緻化：8 階紅-綠漸層取代 4 色分類
1. **8 階評分色階**：路線舒適度上色由粗略的 4 色分類（舒適/普通/欠佳/差）改為每 0.5 分一階的 8 色漸層（採 ColorBrewer RdYlGn 配色），4.5+ 深綠、4.0–4.5 淺綠、3.5–4.0 黃綠、3.0–3.5 淺黃、2.5–3.0 淺橙、2.0–2.5 橙紅、1.5–2.0 紅、1.0–1.5 深紅，色彩過渡更平滑、資訊量更高。
2. **圖例改為連續漸層條**：圖層面板的舒適度圖例由 4 個色塊文字列表改為單條 8 階漸層色條，兩端標註 1.0（差）與 5.0（極佳），版面更精簡。
3. **色階邏輯抽出共用常數**：`COMFORT_SCALE` 定義於 `RouteLayer.js` 並匯出供圖例複用，路段上色與統計橫條圖沿用同一色階函式，確保視覺一致性。

## [v1.9.1] - 2026-07-02 - 📊 - FEATURE - 步行舒適度視覺化：評分資料上色路線與路段統計
1. **步行舒適度檢視模式**：圖層面板新增「🌡️ 步行舒適度檢視」開關，開啟後四條路線改依民眾評分平均上色——綠（舒適 ≥4）、黃（普通 ≥3）、橙（欠佳 ≥2）、紅（差 <2），尚無評分的路段以灰色虛線顯示，附色階圖例。
2. **路段評分統計 Popup**：舒適度模式下點擊任一路段，評分表單上方顯示該路段統計——遮蔭、路面、安全感、整體舒適度四項平均分橫條圖與樣本數，無資料路段顯示邀請評分提示，形成「看見評分→自己也評」的參與迴路。
3. **評分聚合 API**：新增 `/api/route-comfort-stats`，從 GAS 讀取原始評分列，按路線與路段聚合平均分（含 60 秒快取減少後台負載），異常分數（超出 1–5）自動過濾。
4. **路線評分 GAS 範本**：新增 `src/lib/gas-route-template.js`，補齊路線舒適度評分後台的標準腳本（含 doGet 讀取端點與攤平式欄位結構），供重新部署使用。
5. **修復虛線殘留**：修正 Leaflet `setStyle` 合併式更新導致關閉舒適度模式後路段虛線樣式殘留的問題（所有樣式分支明確指定 `dashArray`）。

## [v1.9.0] - 2026-07-02 - ⚠️ - FEATURE - 環境通報機制：回饋類型二分、通報標籤與後台分流審核
1. **回饋性質選擇**：地景標記表單新增「📖 地方記憶 / ⚠️ 環境通報」二選一，通報模式切換為問題導向標籤組（積水、異味、疑似污染排放、缺遮蔭、垃圾堆積、其他），描述欄位與提示文字同步調整為現場觀察導向。
2. **通報標記視覺區分**：審核通過的環境通報在地圖上以 ⚠️ 紅色警示圖標顯示，與 🧡 地方記憶標記明確區分；popup 標頭顯示「⚠️ 環境通報」紅色徽章。
3. **後台分流審核**：管理後台新增類型篩選器（全部／地方記憶／環境通報），每筆回饋顯示類型徽章，待審核清單中通報類自動優先排序，加快環境問題的審核回應速度。
4. **資料欄位擴充**：回饋資料新增 `feedback_type` 欄位，GAS 後台範本（`src/lib/gas-template.js`）同步更新；舊資料自動視為地方記憶，完全向下相容。

## [v1.8.8] - 2026-06-14 - 📡 - UPDATE - iOS Safari HTTPS 偵測：GPS 與語音功能在非安全環境給予明確提示
1. **定位錯誤改為 Inline Toast**：GPS 定位按鈕錯誤訊息由 `alert()` 改為地圖右上角的卡片式提示，可手動關閉，不阻斷操作流程。
2. **isSecureContext 偵測**：點擊定位按鈕時優先檢查 `window.isSecureContext`，若為 HTTP 連線立即顯示「需要 HTTPS」提示，並說明 Safari 設定路徑（iOS 靜默封鎖不詢問的根本原因）。
3. **語音按鈕在 HTTP 下自動禁用**：`NodeFeedbackForm` 的語音辨識偵測加入 `isSecureContext` 判斷，HTTP 環境下語音按鈕顯示灰色禁用狀態並附帶提示，取代原本功能靜默失效。

## [v1.8.7] - 2026-06-14 - 📱 - UI - 手機版面板限高、站點 popup 縮高與新增關閉按鈕
1. **圖層面板手機限高**：展開時限制為 `58dvh`（桌機維持全高），面板下方保留地圖可見空間，不再全遮版面。
2. **站點 Popup 縮高**：`.leaflet-popup-content` 的 `max-height` 由 `70dvh` 縮至 `52dvh`，手機瀏覽站點時地圖不再完全被遮蓋。
3. **站點關閉按鈕**：在每個站點 badge（「站A」「站B」等）右側新增小圓形 ✕ 關閉按鈕，點擊即關閉 popup，方便手機單手操作。

## [v1.8.6] - 2026-06-11 - 🗺️ - UI - 路線圓點優化：柔和邊框、Zoom 自適應大小與字體調大
1. **柔和邊框**：站點圓點邊框由純白（`#fff` weight 2）改為路線深色調的半透明描邊（`colorDark + 40% opacity`），視覺更融入地圖，不再有刺眼白圈。
2. **Zoom 自適應圓點大小**：新增 `ZoomAwareMarker` 元件，圓點半徑隨 zoom 動態調整（zoom ≥ 17: r9 / ≥ 15: r7 / ≥ 13: r5 / < 13: r3），解決 zoom out 時圓點過大遮蓋地圖的問題。
3. **Sidebar 字體調大**：導覽標籤由 `text-[9px]` 改為 `text-xs`（桌機版 `text-sm`），「更新日誌」按鈕字體同步調整，提升可讀性。

## [v1.8.5] - 2026-06-11 - 🎨 - AESTHETICS - 地圖 UI 水系美學對齊：Header、Sidebar、圖層面板與路線色系
1. **Header 淺色水系漸層**：頂欄由深黑玻璃改為天空藍水平漸層（`#f0f9ff → #e0f2fe → #f0f9ff`），標題「信水義河」改用 Noto Serif TC 明體深色字，與 Landing 頁視覺語言統一。
2. **Sidebar 淺藍垂直漸層**：左欄導覽列由深藍半透明改為天藍→水藍漸層白底，按鈕 active 狀態改為 sky-600，inactive 字色改為深灰，整體風格與 Landing 頁保持一致的水系淺色調。
3. **圖層面板輕盈化**：圖層切換面板外框、按鈕及標題改用淺藍系色彩（`border-sky-200`、`bg-sky-50`），標題字改用 Noto Serif TC，視覺上更輕盈自然。
4. **路線顏色改為 Earth Tones**：四條路線的點與線由高飽和 RGB 色改為大地色系——路線一：鋼鐵水藍（`#4580A0`）、路線二：苔蘚綠（`#5C7A52`）、路線三：赭磚棕（`#A05C42`）、路線四：暖褐黃（`#8A724A`），視覺清晰但不刺眼，符合生態人文基調。
5. **導覽 Emoji 更新**：路線標籤 Emoji 由 🔵🟢🟠🟣 改為 🩵🌿🟤🌾，與新的 earth tone 色系呼應。

## [v1.8.3] - 2026-06-10 - 🗺️ - UPDATE - 恢復預設開啟「1939 瑠公水利區域圖」圖層（透明度 55%）
1. **恢復歷史圖層預設值**：修復地圖載入時歷史圖層未自動開啟的問題，恢復預設啟用「1939 瑠公水利區域圖」（`liugong1939`），讓用家一打開地圖即可直接看到古地圖疊圖效果。
2. **透明度調整為 55%**：將 `liugong1939` 的預設透明度由 50% 調整至 55%，使古地圖與現代底圖對比更清晰，同時不遮擋底圖街道細節。

## [v1.8.4] - 2026-06-10 - 🌊 - AESTHETICS - Landing 頁水波視覺重設計：明體字與波光漸層
1. **水藍漸層背景**：將 Landing 頁背景由原本的米白漸層改為天空藍→水藍→霧藍的垂直漸層（`#dbeafe → #bfdbfe → #eff9ff`），呼應信義區水文的核心主題。
2. **波光粼粼動畫**：加入三層 SVG 波形（頂部、中部、底部），搭配 8 個不規則錯開的橢圓光暈，以 `shimmer-pulse` 動畫模擬水面反光的波光粼粼效果，純 CSS 實現無效能負擔。
3. **明體字設計**：主標題「信水義河」、副標題、CTA 按鈕、卡片標題改用 `Noto Serif TC` 明體，配合寬鬆字距（`tracking-[0.22em]`），呈現接近 yenz.tw 參考風格的人文優雅感，保留卡片內文 Sans 字體確保可讀性。
4. **卡片重構**：卡片改以 map 陣列渲染，統一白底半透明玻璃質感（`bg-white/80`）與淡藍邊框，去除多餘色彩干擾，視覺更簡潔。
5. **分隔線設計**：以天藍色漸層短線 + 圓點取代原本的素灰分隔線，呼應水系主題。

## [v1.8.2] - 2026-06-10 - 🧹 - UPDATE - 程式碼清理：刪除死碼與補齊 DATA 標籤樣式
1. **刪除 usage 分頁死碼**：移除 100 行永遠無法到達的「使用方法」overlay（無任何按鈕可切換至該分頁），減少 bundle 大小並消除潛在的維護混淆。
2. **補齊 DATA 標籤樣式**：`getTagStyles()` 新增 `DATA` case（天藍色系），符合 CLAUDE.md 規定的七個合法 Tag，避免資料類更新日誌以灰色 default 呈現。

## [v1.8.1] - 2026-06-10 - 🐛 - UPDATE - 功能 Bug 修復：CHANGELOG 解析、麥克風洩漏與導覽元素
1. **修復 v1.6.0 更新日誌不顯示**：v1.6.0 標題列缺少 Tag 欄位導致解析器跳過整條記錄，補上 `MILESTONE` Tag 後恢復正常顯示。
2. **修復麥克風在 Popup 關閉後持續錄音**：NodeFeedbackForm 新增 unmount cleanup，確保語音辨識 session 在表單關閉時立即停止，防止麥克風背景持續運作。
3. **確認導覽元素 ID 正確**：確認 `#layer-control-panel-content` 已存在於圖層面板，driver.js 引導流程可正確偵測面板展開狀態。

## [v1.8.0] - 2026-06-10 - 🔐 - UPDATE - 後台安全強化：移除硬編碼密碼與 GAS Webhook URL
1. **移除預設管理密碼**：後台 `ADMIN_PASSCODE` 環境變數若未設定，API 即回傳 503 拒絕服務，不再 fallback 至硬編碼字串，防止未妥善設定環境時遭未授權存取。
2. **移除所有硬編碼 GAS Webhook URL**：四個 API Route（`feedback-list`、`feedback-node`、`feedback-route`、`admin/feedback`）的 Google Apps Script fallback URL 全數移除，URL 未設定時回傳 503，避免洩漏的舊 URL 繼續被呼叫。
3. **清除前端 bundle 洩漏風險**：移除所有 `NEXT_PUBLIC_NODE_SHEETS_API_URL` 與 `NEXT_PUBLIC_SHEETS_API_URL` 引用，一律改用僅伺服器端可讀的 `NODE_SHEETS_API_URL` / `SHEETS_API_URL`，確保敏感 URL 不被打包進前端程式碼。

## [v1.7.0] - 2026-06-03 - 🛡️ - LATEST - 隱私聲明專頁、EXIF 隱私控制與 AI 輔助潤飾標示
1. **隱私權聲明專頁 🛡️**：新增精美的 `/privacy` 專屬頁面，採用 premium glassmorphism 視覺風格，詳盡說明資料收集範圍、不收集項目（如姓名、Email、Cookie、語音檔案等）、第三方傳輸對照表（Gemini API、Google Sheets 等）、資料保留期限、內容人工審核制度以及使用者的隱私權利，全方位提升資料可解釋性與信任感。同時，在主頁頂部導覽列與 Welcome Landing Page 底部嵌入直觀的連結。
2. **EXIF GPS 隱私控制選項 🔒**：在回饋表單上傳照片後，若偵測到照片中含有 GPS 座標，系統將自動跳出「🔒 移除照片 GPS 定位資料」切換開關。使用者可一鍵決定是否在上傳前將該照片後端 metadata payload 中的 GPS 定位數據移除，保障實地踏查的個人隱私安全。
3. **AI 輔助潤飾標示 ✨**：在公開地景標記彈出視窗（Popup）中，如果該段地景故事是由 AI 輔助生成/潤飾（`ai_summary` 欄位存在），現在將顯示醒目的「✨ AI 輔助潤飾」精緻標章，並在段落開頭明確註明「AI 輔助潤飾（本段文字經 AI 協助生成）」，落實生成式 AI 的可解釋性與資訊揭露。
4. **製作單位權責校正 🏫**：因應專案發展，將 Landing Page welcome 標題與網站各處頁尾的製作單位資訊正名為「國立臺灣大學建築與城鄉研究所『智慧城市與數位民主』課程團隊（與信義社區大學合作）」，確保學術合作與社區推廣之責權清晰。

## [v1.6.0] - 2026-05-27 - 🗂️ - MILESTONE - Google Sheets 雙向數據通路與管理員審批後台系統
1. **雙向審批系統 📂**：全新打通「前台提交 ➔ Google Sheets 待審核（pending）儲存 ➔ 後台審核 ➔ 前台地圖渲染核准標記」的完整生命週期。地圖將動態從試算表安全拉取並僅公開顯示 `status === 'approved'` 的精緻地標，完美確立了後台回到前台的閉環數據鏈路！
2. **管理員安全審核大廳 `/admin` 🔑**：新增專屬 glassmorphism 風格的管理面板，支援全網頁安全密碼鎖定認證。面板具備動態統計數字（待審核、已核准、已拒絕）與三欄分頁切換，管理員可一鍵執行「核准公開」、「拒絕公開」與「退回待審」等即時同步指令。
3. **EXIF 後端寫入與審查分析器 📸**：修復並極致優化前端 `exifr` 解析流程，移除 pick 清單限制以保證所有設備在各平台皆能完美提取完整 GPS 座標、拍攝設備與時間，並將資料以 `photo_exif` 對象寫入試算表的新增 EXIF 專用欄位中。同時在後台面板中加入「相片 EXIF 分析儀」，管理員可即時比對照片內置 GPS 與使用者申報位置，甚至可一鍵點擊鏈接進行地圖比對，為資料真實性與導覽價值提供強大輔助。
4. **一鍵替換後台 Apps Script 範本 📝**：於專案 `src/lib/gas-template.js` 中特別為使用者整理出最新的 Google Apps Script 範本，支援 Base64 解碼、照片自動上傳 Google Drive、全 EXIF 欄位寫入、多功能 `doGet` JSON 查詢與 `doPost` 單列狀態更新，方便使用者一鍵完美部署升級後台。
5. **前台拖移照片上傳 📥**：特別為網頁版前台回饋表單優化了相片上傳體驗！現在使用者不僅能點擊上傳，更可以直接將電腦中的照片拖移（Drag & Drop）至上傳區域內。上傳區會伴隨平滑的動態發光（Tailwind Transition）與虛線框顏色切換，並自動執行 EXIF 資料解析與高倍率照片壓縮，帶來極致現代且流暢的社群協作體驗。
6. **後台滾動條佈局修復 🐛**：修復了因全域 `html, body { overflow: hidden }` 造成後台頁面無法往下捲動的缺陷，將後台視窗高度鎖定在 `h-screen` 並賦予審核列表獨立滾動區，確保在所有螢幕尺寸下皆能順暢翻閱並點擊審核按鈕。

## [v1.5.2] - 2026-05-27 - 📱 - UPDATE - 手機與網頁版左側欄寬度優化與圖層面板限高與GPS按鈕一致化
1. **左側欄預設開啟與寬度優化（手機與網頁版） 📱**：手機版預設載入時直接開啟左側欄（便於首頁切換），並將每個導覽按鈕除了 Emoji 之外，更以小字體直向排列顯示中文標籤。為了解決網頁版左側欄視覺上過寬的問題，**網頁版寬度由原本的 `md:w-32`（128px）精簡收窄至 `md:w-24`（96px）**，配合整體邊距與間距縮減，使地圖視野在電腦與手機上皆能展現最極致開闊的尊榮美感。
2. **圖層控制面板限高放寬至 65% 🗺️**：將右側圖層控制面板在手機版的最高限制高度提升至 `65dvh`，讓使用者在小螢幕上可以同時清楚看到完整圖層與底部「自由新增地景標記」按鈕，保持最舒服的操作體驗。
3. **GPS定位按鈕等比例一致化 📍**：將 GPS 我的位置按鈕（locate-button）尺寸調整為與圖層面板展開/收合按鈕完全一致（手機版 w-10 h-10，桌機版 w-12 h-12），並自動精確微調了其上方的定位高度間距，使地圖右側所有按鈕在視覺上極致和諧統一。

## [v1.5.1] - 2026-05-27 - 📱 - UPDATE - 手機版回應式佈局與左右側欄收合優化
1. **手機版左側選單收合 ☰**：於頂部導覽列新增「☰ 選單切換」按鈕。手機版預設為收合狀態以最大化地圖視野（由 96px 降至 0px），點擊後可流暢滑動展開，並隱藏標籤文字僅顯示直觀 Emoji 圖示。
2. **手機版圖層控制面板精簡 📱**：圖層面板寬度由 72 縮減至 60，最大高度限制為 50dvh（50% 螢幕高度），保留至少一半的螢幕給地圖，並全方位精簡了間距、內邊距與字體大小。
3. **防遮擋地景標記按鈕 📍**：極致優化最底部「自由新增地景標記」按鈕的內邊距（py-1.5），確保在所有大小的手機螢幕上皆能完全顯示且絕不被遮擋。

## [v1.5.0] - 2026-05-27 - 📸 - UPDATE - AI 圖片轉譯與 EXIF 後台收集 & 圖層面板體驗優化
1. **AI 圖片轉譯 📝**：上傳照片後，一鍵呼叫 Gemini 2.5 Flash 多模態 AI 分析相片文字與環境，並自動產生場景描述填入回饋框。
2. **EXIF 拍照後設資料收集 📍**：在前端利用 `exifr` 於壓縮前解析照片原始 GPS 座標、拍照時間、拍攝裝置，並自動送至後端 Google Sheets 收集（此資訊僅後台紀錄，前端保持隱私不顯示）。
3. **圖層控制面板滾動優化 ☰**：重構右側圖層控制面板為 Flex 結構，「✕ 關閉」按鈕與最底部的「自由新增地景標記」按鈕永遠固定於面板頂部與底部，其餘內容支援流暢捲動，解決小螢幕及電腦版按鈕被遮擋問題。

## [v1.4.0] - 2026-05-25 - ✨ - AESTHETICS - 全新 Landing Page、頂部品牌列與溫暖色調設計
1. **溫暖優雅色調 🎨**：拋棄冷峻的科技感藍色，以「淺藍、溫暖淺啡、奶油色」重構網站主視覺，採用高對比且柔和的台北黑體字型。
2. **專屬 Landing Page 🗺️**：新增大氣的探索導航頁面，為五大對象（導覽學員、自主踏查、時空對照、社區居民、數位共創）精心提煉網站使用場景與價值，並將「展開探索」按鈕置於副標題黃金位置。
3. **頂部品牌 header 欄 🏢**：頂欄左邊增加「信水義河 信義社大 水文導覽地圖」品牌識別，導航按鈕精簡為「首頁」與左下角「使用方法」，介面更加簡潔開闊。

## [v1.3.0] - 2026-05-22 - 🎙️ - UPDATE - 智慧地景回饋與 AI 故事潤飾 (語音辨識 + AI 整理 + 體驗優化)
1. **語音輸入 🎙️**：走讀時只需動口，即可輕鬆將語音轉為繁體中文文字。
2. **Gemini AI 潤飾 ✨**：整合 Google Gemini 2.5 Flash 技術，一鍵將口語零碎的回憶整理成溫暖流暢的地景故事。
3. **彈出視窗防誤關機制 (Bug Fix) 🐛**：修復了因語音或 AI 卡片 React 節點卸載造成 Leaflet 地圖彈出視窗自動關閉的缺陷，確保回饋填寫過程順暢無阻。

## [v1.2.0] - 2026-05-21 - 🌡️ - FEATURE - 地表溫度圖層 (Landsat 8) 新增透明度滑桿
1. 在衛星地表溫度圖層控制中新增了平滑的透明度拉桿。使用者可以自由調節 Landsat 8 熱島效應分布圖的覆蓋透明度（從 0% 至 100%），方便比對地表實際地貌與熱量分布，提升水文與都市熱島的綜合研究體驗。

## [v1.1.2] - 2026-05-21 - 🔧 - UPDATE - 圖層工具提示資訊圖示
1. 在圖層控制面板中每個圖層的勾選框右側，新增了 ℹ️ 圓形資訊圖示按鈕。點擊後彈出詳細說明彈窗，說明該圖層的來源、時期與用途，讓使用者無需離開地圖即可了解圖層背景。

## [v1.1.0] - 2026-05-19 - 🗂️ - UI - 「資料來源」按鈕位置調整
1. 將「資料來源 ℹ️」控制按鈕從地圖上方移至左下角，位於「使用方法」按鈕正上方，解決了按鈕遮擋圖層下拉式選單的問題。同時調整展開動畫原點，確保彈出方向自然。

## [v1.0.5] - 2026-05-19 - 🏠 - UI - 側欄標題整合與地圖介面簡化
1. 將地圖左下角浮動的「信水義河 信義社大 · 水文導覽互動地圖」標題文字，整合至左側側欄頂部作為品牌識別。地圖底部僅保留「❓ 使用方法」單一按鈕，讓地圖畫面更加簡潔開闊。

## [v1.0.0] - 2026-05-19 - 🚀 - MILESTONE - 水文回饋表單 A & B 與後台正式串接
1. 正式發佈 v1.0.0 穩定里程碑！推出路線舒適度評分（表單 A）與自由標記故事回饋（表單 B）。使用者可於地圖任意點選落點、選取標籤並上傳現場相片（經前端智慧壓縮）。回饋資料透過 Next.js 後端 API Proxy 安全轉發，自動寫入 Google Sheets 後台並儲存相片至雲端硬碟。

## [v0.9.5] - 2026-05-18 - 📋 - FEATURE - Driver.js 導覽教學與都市計畫分區透明度
1. 整合 Driver.js 實作 9 步驟互動式新手導覽，引導使用者認識路線、歷史圖層與定位功能，降低學習門檻。同時為「都市計畫分區」圖層新增 0-100% 透明度調整滑桿，使多圖層疊加對照體驗更臻完善。

## [v0.9.0] - 2026-05-13 - 🛰️ - UX - 多光譜衛星影像整合與全域字體動態調整
1. 整合 Sentinel-2 衛星影像，提供真彩色、植被健康指數（NDVI）與濕度指數（NDWI）三種光譜分析模式。此外，於側欄新增全域字體大小調節器（小 / 中 / 大），運用 CSS 自訂變數動態縮放全頁字體比例，大幅提升可讀性與無障礙操作體驗。

## [v0.8.5] - 2026-05-12 - 🕰️ - FEATURE - 歷史地圖不透明度滑桿控制
1. 為所有疊合的古地圖圖層導入獨立的不透明度滑動拉桿（0-100%），讓使用者能微調疊合比例，直觀比對清代、日治與戰後等不同時期的信義區地理水文變遷。

## [v0.8.0] - 2026-05-07 - 🌡️ - DATA - Google Earth Engine (GEE) 地表溫度整合
1. 首度透過 Google Earth Engine 整合即時地表溫度（LST）熱力圖，可視化信義區都市熱島效應分布與水體冷卻效應，為水文與環境走讀研究提供量化科學依據。

## [v0.7.0] - 2026-05-06 - 🗺️ - DATA - 五層歷史古地圖疊加與路線結構優化
1. 整合 1904 臺灣堡圖、1921 地形圖、1939 瑠公水利區域圖、1944 美軍地形圖及 1989 地形圖等五張珍貴古地圖，支援一鍵切換。同時修正水文路線斷點缺口，大幅優化坐標連貫性。

## [v0.6.0] - 2026-04-30 - 🌳 - DATA - 行道樹遮蔭圖層與點群聚 (Clustering) 整合
1. 匯入信義區行道樹開放資料，並實作 MarkerClusterGroup 群聚渲染，高效率在地圖上呈現樹木遮蔭分布與單木詳細樹種資訊，避免地圖標記擁擠卡頓。

## [v0.5.0] - 2026-04-29 - 🌱 - DATA - 信水義河水文互動地圖基礎框架建立
1. 完成信水義河地圖專案初始化！以 Next.js 16 + TailwindCSS v4 搭建明亮簡潔的地圖版面，整合 Leaflet 與 OpenStreetMap 基礎圖資底圖，完成四大水文導覽路線之核心繪製與站點座標串接。
